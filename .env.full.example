# Compose todo-en-uno (docker-compose.full.yml)
POSTGRES_PASSWORD=      # openssl rand -hex 16
EVOLUTION_API_KEY=      # openssl rand -hex 24  (key global de Evolution)
JWT_SECRET=             # openssl rand -hex 32
ENCRYPTION_KEY=         # openssl rand -hex 32  (exactamente 64 chars hex)
WEBHOOK_SECRET=         # openssl rand -hex 24
PUBLIC_URL=http://IP_DEL_VPS:3000
TZ=America/Guayaquil
